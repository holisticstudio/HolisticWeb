{
  "name": "holistic-back",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "NODE_ENV=dev nodemon index.js",
    "build:ui": "rm -rf dist && cd ../holistic-started/ && npm run build && cp -r dist ../Holistic-backend"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cross": "^1.0.0",
    "env": "^0.0.2",
    "nodemon": "^3.1.7"
  },
  "dependencies": {
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "express-static-gzip": "^2.2.0",
    "jsonwebtoken": "^9.0.2",
    "multer": "^1.4.5-lts.1",
    "nodemailer": "^6.9.16"
  }
}
